@Library('jenkins-shared-lib@main') _
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                buildApplication(appName: 'app1')
            }
        }
    }
}
